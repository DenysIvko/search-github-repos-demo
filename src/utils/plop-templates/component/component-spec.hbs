import React from 'react';
import { shallow } from 'enzyme';

import {{name}} from '../{{name}}';

describe('<{{name}} />', () => {
  test('renders', () => {
    const tree = shallow(
      <{{name}} />
    );
    expect(tree).toMatchSnapshot();
  });
});
