/* ACTION CONSTANTS */

/* REDUCER */

const initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
}

/* SELECTORS */

/* ACTION CREATORS */

/* THUNKS */
